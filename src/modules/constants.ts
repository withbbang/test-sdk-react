import { CustomWindow } from './types';

export const CUSTOM_WINDOW = window as CustomWindow;

export const RETURN_URL = process.env.REACT_APP_RETURN_URL;

export const MODE = process.env.REACT_APP_MODE;
